---
section: ccip
date: Last Modified
title: "Deploy and Register your Cross-Chain Tokens Using Remix"
metadata:
  description: "Learn how to deploy and register cross-chain tokens in CCIP using Remix IDE and an EOA account. Step-by-step guide for configuring token pools and rate limits."
---

import { Aside, ClickToZoom, CodeSample, CopyText } from "@components"
import { Tabs } from "@components/Tabs"
import {
  TutorialBlockchainSelector,
  ChainValue,
  ContractAddress,
  NetworkAddress,
  StoredContractAddress,
  ChainUpdateBuilder,
  PoolConfigVerification,
} from "@components/CCIP/TutorialBlockchainSelector"
import { ChainUpdateBuilderWrapper } from "@components/CCIP/TutorialBlockchainSelector/ChainUpdateBuilderWrapper"
import { StepCheckbox } from "@components/CCIP/TutorialProgress/StepCheckbox"
import { PrerequisitesCard } from "@components/CCIP/TutorialSetup/PrerequisitesCard"
import { SetupSection } from "@components/CCIP/TutorialSetup/SetupSection"

This tutorial will guide you through enabling your tokens in CCIP using only your web browser and [Remix IDE](https://remix.ethereum.org). You'll learn how to deploy tokens and set up token pools without needing to install any development tools.

<Aside type="note" title="Before You Start">
  To better understand Cross-Chain Token (CCT) standard:

        - Read the [CCT Standard documentation](/ccip/concepts/cross-chain-tokens) for a comprehensive overview of the architecture, components, and
    security features
        - Check the [CCT Guides overview](/ccip/tutorials/cross-chain-tokens) to understand the general workflow for deploying and enabling your tokens in CCIP

</Aside>

## Before You Begin

<PrerequisitesCard client:load />

<SetupSection
  title="Select Your Blockchains"
  description="Choose the source and destination blockchains for your cross-chain token"
>
  <TutorialBlockchainSelector client:load />
</SetupSection>

<SetupSection 
  title="Import Required Contracts"
  description="Import and compile the token contracts in Remix IDE"
  checkbox={{ 
    stepId: "setup", 
    subStepId: "contracts-imported"
  }}
  client:only="react"
>
  1. Open the pre-configured token contract in Remix:

{" "}
<CodeSample src="samples/CCIP/cct/TokenDependencies.sol" showButtonOnly={true} optimize={true} runs={1000} />

2. Wait a few seconds for Remix to automatically compile all contracts.
   </SetupSection>

## Tutorial

### Source Blockchain Setup

<Aside type="note" title="Already Have a Token?">
  If you have an existing token that meets the [CCT requirements](/ccip/concepts/cross-chain-tokens#requirements-for-cross-chain-tokens):

- Skip the "Deploy Token" section
- Enter your existing token address in the address field below
- Continue with "Claim and Accept Admin Role"

The tutorial will use your provided token address for subsequent steps.

</Aside>

#### Deploy Token

1. Ensure MetaMask is still connected to the source blockchain: <ChainValue type="source" bold required={false} client:load />

1. In the "Deploy & Run Transactions" tab:

   - Set Environment to "Injected Provider - MetaMask"
   - Select **BurnMintERC677** contract
   - Configure deployment parameters:
     - `name`: Your token's full name (e.g., "My Cross Chain Token")
     - `symbol`: A short identifier for your token (e.g., "MCCT")
     - `decimals`: Number of decimal places for token amounts (e.g., 18)
     - `maxSupply`: Maximum total supply in wei (use 0 for unlimited supply)
   - Click "Deploy" and confirm in MetaMask
   - Your new token address will be displayed in the "Deployed Contracts" section
   - Save your token address:<br/>
     <div style={{ marginTop: "0.5rem" }}>
       <ContractAddress type="token" chain="source" placeholder="Enter deployed token address" client:load />
     </div>

#### Claim and Accept Admin Role

1. Ensure MetaMask is still connected to the source blockchain: <ChainValue type="source" bold required={false} client:load />

1. Register as admin:

   - In the "Deploy & Run Transactions" tab, select **RegistryModuleOwnerCustom** contract
   - Use "At Address" with the RegistryModuleOwnerCustom contract: <div style={{ display: "inline" }}><NetworkAddress type="registryModule" chain="source" required={false} client:only="react" /></div>
   - The RegistryModuleOwnerCustom will be displayed in the "Deployed Contracts" section
   - Click on the RegistryModuleOwnerCustom contract address to open the contract details
   - Call `registerAdminViaOwner` with your token address: <div style={{ display: "inline" }}><StoredContractAddress type="token" required={false} chain="source" client:only="react" /></div>
   - Confirm the transaction in MetaMask

   <StepCheckbox stepId="sourceChain" subStepId="admin-claimed" client:only="react" />

1. Accept the admin role:

   - In the "Deploy & Run Transactions" tab, select **TokenAdminRegistry** contract
   - Use "At Address" with the TokenAdminRegistry contract: <div style={{ display: "inline" }}><NetworkAddress type="tokenAdminRegistry" chain="source" required={false} client:only="react" /></div>
   - The TokenAdminRegistry will be displayed in the "Deployed Contracts" section
   - Click on the TokenAdminRegistry contract address to open the contract details
   - Call `acceptAdminRole` with your token address: <div style={{ display: "inline" }}><StoredContractAddress type="token" required={false} chain="source" client:only="react" /></div>
   - Confirm the transaction in MetaMask

   <StepCheckbox stepId="sourceChain" subStepId="admin-accepted" client:only="react" />

#### Deploy Token Pool

1. Ensure MetaMask is still connected to source blockchain: <ChainValue type="source" bold required={false} client:load />

1. Choose and deploy your token pool type:

<Tabs sharedStore="poolType" client:visible>
  <Fragment slot="tab.1">Lock & Release Pool</Fragment>
  <Fragment slot="tab.2">Burn & Mint Pool</Fragment>
  <Fragment slot="panel.1">
    
        Select **LockReleaseTokenPool** contract and configure deployment parameters:

            - `tokenAddress`: Your deployed token address <StoredContractAddress type="token" chain="source" required={false} client:only="react" />
            - `localTokenDecimals`: Number of decimals for your token (e.g., 18)
            - `allowlist`: Empty array `[]` to allow any address to transfer tokens, or specify addresses to restrict who can initiate transfers
            - `rmnProxyAddress`: ARM Proxy contract <NetworkAddress type="armProxy" chain="source" required={false} client:only="react" />
            - `acceptLiquidity`: When `true`, enables external liquidity for lock/release mechanism
            - `router`: CCIP Router contract <NetworkAddress type="router" chain="source" required={false} client:only="react" />

  </Fragment>
  <Fragment slot="panel.2">
        Select **BurnMintTokenPool** contract and configure deployment parameters:

            - `tokenAddress`: Your deployed token address <StoredContractAddress type="token" chain="source" required={false} client:only="react" />
            - `localTokenDecimals`: Number of decimals for your token (e.g., 18)
            - `allowlist`: Empty array `[]` to allow any address to transfer tokens, or specify addresses to restrict who can initiate transfers
        - `rmnProxy`: ARM Proxy contract <NetworkAddress type="armProxy" chain="source" required={false} client:only="react" />
        - `router`: CCIP Router contract <NetworkAddress type="router" chain="source" required={false} client:only="react" />

  </Fragment>
</Tabs>

After configuring:

- Click "Deploy" and confirm in MetaMask
- Save your pool address:<br/>

  <div style={{ marginTop: "0.5rem" }}>
    <ContractAddress type="tokenPool" chain="source" placeholder="Enter deployed pool address" client:load />
  </div>

#### Set Token Pool in Registry

1. Ensure MetaMask is still connected to the source blockchain: <ChainValue type="source" bold required={false} client:load />

1. Configure the registry:

   - In the list of deployed contracts, select **TokenAdminRegistry** contract at <NetworkAddress type="tokenAdminRegistry" chain="source" required={false} client:only="react" />
   - Call `setPool` with:
     - your token address <StoredContractAddress type="token" chain="source" required={false} client:only="react" />
     - your token pool address <StoredContractAddress type="tokenPool" chain="source" required={false} client:only="react" />

   <StepCheckbox stepId="sourceChain" subStepId="pool-registered" client:only="react" />

### Destination Blockchain Setup

<Aside type="note" title="Already Have a Token?">
  If you have an existing token that meets the [CCT requirements](/ccip/concepts/cross-chain-tokens#requirements-for-cross-chain-tokens):

- Skip the "Deploy Token" section
- Enter your existing token address in the address field below
- Continue with "Claim and Accept Admin Role"

The tutorial will use your provided token address for subsequent steps.

</Aside>

#### Deploy Token

1. Ensure MetaMask is connected to the destination blockchain: <ChainValue type="destination" bold required={false} client:load />

1. In the "Deploy & Run Transactions" tab:

   - Set Environment to "Injected Provider - MetaMask"
   - Select **BurnMintERC677** contract
   - Configure deployment parameters:
     - `name`: Same name as source token (e.g., "My Cross Chain Token")
     - `symbol`: Same symbol as source token (e.g., "MCCT")
     - `decimals`: Same decimals as source token (e.g., 18)
     - `maxSupply`: Maximum total supply in wei (use 0 for unlimited supply)
   - Click "Deploy" and confirm in MetaMask
   - Your new token address will be displayed in the "Deployed Contracts" section
   - Save your token address:<br/>
     <div style={{ marginTop: "0.5rem" }}>
       <ContractAddress type="token" chain="destination" placeholder="Enter deployed token address" client:load />
     </div>

#### Claim and Accept Admin Role

1. Ensure MetaMask is still connected to the destination blockchain: <ChainValue type="destination" bold required={false} client:load />

1. Register as admin:

   - In the "Deploy & Run Transactions" tab, select **RegistryModuleOwnerCustom** contract
   - Use "At Address" with the RegistryModuleOwnerCustom contract: <div style={{ display: "inline" }}><NetworkAddress type="registryModule" chain="destination" required={false} client:only="react" /></div>
   - The RegistryModuleOwnerCustom will be displayed in the "Deployed Contracts" section
   - Click on the RegistryModuleOwnerCustom contract address to open the contract details
   - Call `registerAdminViaOwner` with your token address: <div style={{ display: "inline" }}><StoredContractAddress type="token" required={false} chain="destination" client:only="react" /></div>
   - Confirm the transaction in MetaMask

   <StepCheckbox stepId="destinationChain" subStepId="admin-claimed" client:only="react" />

1. Accept the admin role:

   - In the "Deploy & Run Transactions" tab, select **TokenAdminRegistry** contract
   - Use "At Address" with the TokenAdminRegistry contract: <div style={{ display: "inline" }}><NetworkAddress type="tokenAdminRegistry" chain="destination" required={false} client:only="react" /></div>
   - The TokenAdminRegistry will be displayed in the "Deployed Contracts" section
   - Click on the TokenAdminRegistry contract address to open the contract details
   - Call `acceptAdminRole` with your token address: <div style={{ display: "inline" }}><StoredContractAddress type="token" required={false} chain="destination" client:only="react" /></div>
   - Confirm the transaction in MetaMask

   <StepCheckbox stepId="destinationChain" subStepId="admin-accepted" client:only="react" />

#### Deploy Token Pool

1. Ensure MetaMask is still connected to destination blockchain: <ChainValue type="destination" bold required={false} client:load />

1. Choose and deploy your token pool type:

<Tabs sharedStore="poolType" client:visible>
  <Fragment slot="tab.1">Lock & Release Pool</Fragment>
  <Fragment slot="tab.2">Burn & Mint Pool</Fragment>
  <Fragment slot="panel.1">
    
        Select **LockReleaseTokenPool** contract and configure deployment parameters:

            - `tokenAddress`: Your deployed token address <StoredContractAddress type="token" chain="destination" required={false} client:only="react" />
            - `localTokenDecimals`: Number of decimals for your token (e.g., 18)
            - `allowlist`: Empty array `[]` to allow any address to transfer tokens, or specify addresses to restrict who can initiate transfers
            - `rmnProxyAddress`: ARM Proxy contract <NetworkAddress type="armProxy" chain="destination" required={false} client:only="react" />
            - `acceptLiquidity`: When `true`, enables external liquidity for lock/release mechanism
            - `router`: CCIP Router contract <NetworkAddress type="router" chain="destination" required={false} client:only="react" />

  </Fragment>
  <Fragment slot="panel.2">
        Select **BurnMintTokenPool** contract and configure deployment parameters:

            - `tokenAddress`: Your deployed token address <StoredContractAddress type="token" chain="destination" required={false} client:only="react" />
            - `localTokenDecimals`: Number of decimals for your token (e.g., 18)
            - `allowlist`: Empty array `[]` to allow any address to transfer tokens, or specify addresses to restrict who can initiate transfers
        - `rmnProxy`: ARM Proxy contract <NetworkAddress type="armProxy" chain="destination" required={false} client:only="react" />
        - `router`: CCIP Router contract <NetworkAddress type="router" chain="destination" required={false} client:only="react" />

  </Fragment>
</Tabs>

After configuring:

- Click "Deploy" and confirm in MetaMask
- Save your pool address:<br/>

  <div style={{ marginTop: "0.5rem" }}>
    <ContractAddress type="tokenPool" chain="destination" placeholder="Enter deployed pool address" client:load />
  </div>

#### Set Token Pool in Registry

1. Ensure MetaMask is still connected to the destination blockchain: <ChainValue type="destination" bold required={false} client:load />

1. Configure the registry:

   - In the list of deployed contracts, select **TokenAdminRegistry** contract at <NetworkAddress type="tokenAdminRegistry" chain="destination" required={false} client:only="react" />
   - Call `setPool` with:
     - your token address <StoredContractAddress type="token" chain="destination" required={false} client:only="react" />
     - your token pool address <StoredContractAddress type="tokenPool" chain="destination" required={false} client:only="react" />

   <StepCheckbox stepId="destinationChain" subStepId="dest-pool-registered" client:only="react" />

### Configure Cross-Chain Communication

#### Source Chain Configuration

Ensure MetaMask is still connected to the source blockchain: <ChainValue type="source" bold required={false} client:load />

##### Grant Burn and Mint Privileges

<Aside type="note">Skip this section if you deployed a LockReleaseTokenPool</Aside>

Grant privileges to your token pool:

- In the list of deployed contracts, select your **BurnMintERC677** token at <StoredContractAddress type="token" chain="source" required={false} client:only="react" />
- Call `grantMintAndBurnRoles` with your token pool address: <StoredContractAddress type="tokenPool" chain="source" required={false} client:only="react" />
- Confirm the transaction in MetaMask

<StepCheckbox stepId="sourceConfig" subStepId="source-privileges" client:only="react" />

##### Configure Remote Pool

Call the following function on your Token Pool contract to configure cross-chain communication:

<ChainUpdateBuilderWrapper chain="source" client:load />

<StepCheckbox stepId="sourceConfig" subStepId="source-pool-config" client:only="react" />

##### Verify Source Configuration

Call these functions on your Token Pool contract to verify your configuration:

<PoolConfigVerification chain="source" client:load />

<StepCheckbox stepId="sourceConfig" subStepId="source-verification" client:only="react" />

#### Destination Chain Configuration

Ensure MetaMask is still connected to the destination blockchain: <ChainValue type="destination" bold required={false} client:load />

##### Grant Burn and Mint Privileges

<Aside type="note">Skip this section if you deployed a LockReleaseTokenPool</Aside>

Grant privileges to your token pool:

- In the list of deployed contracts, select your **BurnMintERC677** token at <StoredContractAddress type="token" chain="destination" required={false} client:only="react" />
- Call `grantMintAndBurnRoles` with your token pool address: <StoredContractAddress type="tokenPool" chain="destination" required={false} client:only="react" />
- Confirm the transaction in MetaMask

<StepCheckbox stepId="destConfig" subStepId="dest-privileges" client:only="react" />

##### Configure Remote Pool

Call the following function on your Token Pool contract to configure cross-chain communication:

<ChainUpdateBuilderWrapper chain="destination" client:load />

<StepCheckbox stepId="destConfig" subStepId="dest-pool-config" client:only="react" />

##### Verify Destination Configuration

Call these functions on your Token Pool contract to verify your configuration:

<PoolConfigVerification chain="destination" client:load />

<StepCheckbox stepId="destConfig" subStepId="dest-verification" client:only="react" />
