---
import MainLayout from "~/layouts/MainLayout.astro"
import { getEntry } from "astro:content"
const indexEntry = await getEntry("blockchain-integrations-framework", "index")
const { Content } = await indexEntry.render()
---

<MainLayout>
  <Content />
</MainLayout>
