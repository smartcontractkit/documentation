---
import DocsLayout from "~/layouts/DocsLayout.astro"
import { getEntry } from "astro:content"
const entry = await getEntry("chainlink-functions", "index")
const { Content } = await entry.render()
---

<DocsLayout frontmatter={entry.data}>
  <Content />
</DocsLayout>
