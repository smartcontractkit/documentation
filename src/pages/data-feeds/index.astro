---
import DocsLayout from "~/layouts/DocsLayout.astro"
import { getEntry } from "astro:content"
const entry = await getEntry("data-feeds", "index")
const { Content } = await entry.render()
---

<DocsLayout frontmatter={entry.data}>
  <Content />
</DocsLayout>
