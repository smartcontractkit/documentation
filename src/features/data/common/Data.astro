---
const SelectingDataQuality = await Astro.glob("./selecting-data-quality.mdx")
const SelectingDataQualityComponent = SelectingDataQuality[0].Content

export type Props = {
  section?: "SelectingDataQuality"
}
const { section } = Astro.props as Props
---

{section === "SelectingDataQuality" && <SelectingDataQualityComponent />}
