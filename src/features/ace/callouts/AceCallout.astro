---
import PrivateBetaComponent from "./private-beta.mdx"

export type Props = {
  callout?: "privateBeta"
}

const { callout } = Astro.props as Props
---

{callout === "privateBeta" && <PrivateBetaComponent />}
