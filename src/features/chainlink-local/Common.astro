---
const ImportPackage = await Astro.glob("./ImportPackage.mdx")
const ImportPackageComponent = ImportPackage[0].Content
export type Props = {
  callout?: "importPackage"
}
const { callout } = Astro.props as Props
---

{callout === "importPackage" && <ImportPackageComponent />}
