---
const localTestingBenefits = await Astro.glob("./local-testing-benefits.mdx")
const LocalTestingBenefitsComponent = localTestingBenefits[0].Content

export type Props = {
  section?: "localTestingBenefits"
}
const { section } = Astro.props as Props
---

{section === "localTestingBenefits" && <LocalTestingBenefitsComponent />}
