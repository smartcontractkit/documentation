---
const bridgeRisks = await Astro.glob("./read-bridge-risks.mdx")
const BridgeRisksComponent = bridgeRisks[0].Content

export type Props = {
  callout?: "bridgeRisks"
}
const { callout } = Astro.props as Props
---

{callout === "bridgeRisks" && <BridgeRisksComponent />}
