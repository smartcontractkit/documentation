# TODO: linkcheck is not playing well with some css files generated at build time
assets\/.*.css$
_astro\/.*.css$

# Exclude /ccip/directory/* URLs from internal link checking
\/ccip\/directory\/.*

# Exclude data-feeds price-feeds addresses pages
\/data-feeds\/price-feeds\/addresses\?.*

# Exclude data-feeds smartdata addresses pages
\/data-feeds\/smartdata\/addresses\?.*

# Exclude generated .md files
\/.md$

# Exclude v1 legacy pages
\/ccip\/v1\/.*

# Exclude CCIP Tools docs — served by an external service at docs.chain.link/ccip/tools/*,
# not by this repo (verified live 200s in production); linkcheck rewrites the absolute URLs
# to localhost where they 404
\/ccip\/tools\/.*
\/ccip\/tools$

sitemap-index.xml
@vite/client
