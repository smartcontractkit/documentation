---
import styles from "./DocCards.module.css"

interface Props {
  class?: string
  className?: string
}

const { class: classAttr, className, ...attrs } = Astro.props
---

<div {...attrs} class:list={[styles.cards, classAttr, className]}>
  <slot />
</div>
