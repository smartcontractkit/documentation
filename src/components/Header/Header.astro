---
import NotificationBanner from "~/features/notifications/components/NotificationBanner.astro"
import { Sections } from "~/content/config"
import { NavBarView } from "@chainlink/components"

export type Props = {
  section?: Sections
}
const { section } = Astro.props

// TODO: Replace env for imgix in utils
// TODO: Fix top of sticky elements on mobile (set new height for --theme-navbar-height(?))
// TODO: Work with collapsable property (use grid somehow(?))
// TODO: Work with topLevelNavigation (use next/router(?))
// TODO: Hide the LeftSidebar content on mobile (docs layout)

const path = Astro.url.pathname
---

<!-- Google Tag Manager (noscript) -->
<noscript></noscript><iframe
  src="https://www.googletagmanager.com/ns.html?id=GTM-N6DQ47T"
  height="0"
  width="0"
  style="display:none;visibility:hidden"></iframe>
<!-- End Google Tag Manager (noscript) -->
<NavBar path={path} showSearch={path !== "/"} client:idle />
<NotificationBanner />
